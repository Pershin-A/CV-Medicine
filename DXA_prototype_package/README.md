# DXA prototype

Файлы:
- `DXA_prototype.ipynb` — основной Jupyter Notebook.
- `requirements.txt` — зависимости.

По умолчанию используется:
`C:\Users\Андрей\Downloads\Датасет`

Перед запуском проверьте пути в секции `1. Импорты и конфигурация`.

Ожидаемые файлы разметки:
- `Размеченные/labels.csv` — LEG/SPINE;
- `metal_labels.csv` — metal 0/1;
- `iliac_masks.csv` — пути к маскам подвздошных костей.
